RewriteEngine On

DirectoryIndex index.html
Options -Indexes

# Use one permanent canonical origin for search engines and shared links.
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} !^www\.sigwanuniverse\.com$ [NC]
RewriteRule ^ https://www.sigwanuniverse.com%{REQUEST_URI} [R=301,L]
